# Android build dependencies (PySide6 + full app stack).
# Cross-compile PySide6 wheels before deploy — see docs/android-apk-plan.md
PySide6>=6.8.0
Pillow>=9.0.0
reportlab>=4.2.5
chardet
openpyxl>=3.1.0
et_xmlfile
